import React from 'react';

const Logo = props => (
	<div className="logo">
		<span className="uppercase name">
		Roberta Salerno
		</span>
		<span>
		psicologa psicoterapeuta
		</span>
	</div>
);

export default Logo;